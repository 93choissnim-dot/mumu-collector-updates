# v0.7.60 session workflow and game recovery

Approved intent: finish the compact black/gold desktop workflow, move unfinished work beside run controls, distinguish safe continuation from uncertain resource requests, and recover the game app after a proven exit. Customer support and sales preparation are out of scope. User has authorized autonomous design, implementation, verification and publication without repeated approvals.

Architecture: keep existing vision, collectors, input evidence and ledgers. Add pure session/resume classification shared by UI and worker; a focused UI mixin owns footer scope selector, continuation preview and latest-run view. Keep a compact header and two primary run controls. Select Regular / Daily / All scope independently; automatic execution is explicitly regular-only, never repeat daily jobs. Current scope is visible and persisted. Old daily selections default to all free daily categories and remain configurable. Existing retry actions remain available in details.

Continuation uses enabled account/profile and current KST day. Confirmed terminal tasks excluded. Any pending input, uncertain/blocked daily step, attempted/deferred result, or blocked regular action is held for inspection, never automatically rearmed. Resume preserves held records and completed results rather than resetting the journal. New runs retain unfinished tasks outside their chosen scope. Current-run rows use recorded run entries and planned rooms; older history is explicitly labeled. Counts remain top-level task outcomes, not taps or currency. Viewing account and actively running account are separate.

Game recovery: exact com.nns.genesis only, local previously-identified MuMu endpoint. Prove foreground is resolved Android HOME and target process absent before launching an exact resolved LAUNCHER component. No force-stop, login clicks, blind coordinates, another app takeover or emulator restart. At most two launches per per-device execution, at most 90 seconds active waiting per launch; each command has a timeout. Confirm same package and stable recognized game screen before binding/continuing. Unknown/loading/authentication screens wait without input then stop with a reason. User stop/pause gates launch just like touch. Read-only inspection never launches. Keep same collector, action state, daily ledger, identity and pending requests after recovery. Completed or uncertain tasks are not repeated. Revalidate date before continuation; cannot verify an in-game character switch under unchanged account profile, so profile is identity boundary.

Verification: retain all 844 release tests; add behavior tests for classification, journal preservation and cross-day exclusion, selected scope routing, launch preconditions/bounds/cancellation, worker recovery preserving uncertainty. Real Windows screenshots and widget bounds at 680px and display scaling, idle/running/paused/unfinished/empty/multi-player states. Full frozen EXE, update/restart/rollback gates, draft byte checks, publish exact tested head and verify both feeds. No claim of real-user MuMu gameplay validation; all captured CI screenshots synthetic. No private diagnostic publication.
